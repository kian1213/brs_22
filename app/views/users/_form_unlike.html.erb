<% unless @activity.nil? %>
  <% activity_id ||= @activity.id %>
<% end %>
<% like = Like.current_like activity_id, current_user.id %>
<%= link_to t(".unlike"), like_path(like.first), remote: true,
  method: :delete, class: "btn btn-xs btn-danger" %>
